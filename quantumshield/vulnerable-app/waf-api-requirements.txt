# Requirements for ShopVuln WAF API Service
# Install with: pip install -r waf-api-requirements.txt

# Web Framework (choose one or both)
flask>=2.3.0
flask-cors>=4.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# QuantumShield WAF dependencies
# Note: These should already be installed if QuantumShield is set up
# But listing here for reference

# Core dependencies (if not already installed)
python-dateutil>=2.8.2
pytz>=2023.3

# Optional: For better performance
# gevent>=23.9.0
# gunicorn>=21.2.0
